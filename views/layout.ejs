<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="http://cdnjs.cloudflare.com/ajax/libs/jquery/1.7/jquery.min.js" type="text/javascript"></script>
  </head>
  <body>
    <div class="wrapper">
        <header class="cf">
            <h1><a href="/">shr</a></h1>
            <nav>
                <ul>
                    <li><a href="/create">create</a></li>
                </ul>
            </nav>
        </header>

        <div class="body">
        <%- body %> 
        </div>
    </div>
    <script type="text/javascript">
        $(function() {
            $('select#shrType').change(function() {
                var type = $(this).val();
                var $box = $($('div.frm').first());
                $box.slideUp(function() {
                    $box.load('/tmpl/new/' + type, function() {
                        $box.slideDown();
                    });
                });
            });
        });
    </script>
  </body>
</html>
